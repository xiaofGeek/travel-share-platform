# 环境检测结果

检测日期：2026-07-27  
检测目录：`C:\Users\XF\Desktop\code`

本次检测仅执行只读命令，没有升级软件或修改系统环境变量。

| 命令 | 结果 | 当前终端直接调用 |
| --- | --- | --- |
| `java -version` | PATH 未找到；绝对路径检测为 OpenJDK `21.0.3` | 否 |
| `javac -version` | PATH 未找到；绝对路径检测为 `javac 21.0.3` | 否 |
| `mvn -version` | PATH 未找到；定位后为 Maven `3.9.8` | 否 |
| `node -v` | `v24.11.0` | 是 |
| `npm -v` | PowerShell 执行策略阻止 `npm.ps1`；`npm.cmd -v` 为 `11.6.1` | 使用 npm.cmd |
| `mysql --version` | MySQL Community Server `8.0.46` | 是 |
| `where java / javac / mvn` | 未找到 | 否 |
| `where node` | `E:\node\node.exe` | 是 |
| `where npm` | `E:\node\npm`、`E:\node\npm.cmd` | 是 |
| `where mysql` | `C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe` | 是 |

## 已定位工具

- JDK：IntelliJ IDEA 2024.2 自带完整 JDK，OpenJDK `21.0.3`。
- Maven：IntelliJ IDEA 2024.2 自带 Maven `3.9.8`，实际使用 Java `21.0.3`。
- Node.js：`24.11.0`；npm：`11.6.1`。
- MySQL：`8.0.46`，Windows 服务 `MySQL80` 状态为 Running，自动启动。

## 兼容结论

环境可满足项目开发。PATH 缺少 JDK/Maven 和 PowerShell 禁止 `npm.ps1` 属于命令发现问题，不是版本冲突；项目通过本地工具发现脚本、Maven Wrapper 和 `npm.cmd` 解决，不修改全局设置。

